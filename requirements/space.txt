# Hugging Face Spaces inject gradio, spaces, uvicorn, websockets, and a torch upper bound.
# Keep the Space-specific requirements focused on the serving/runtime slice to avoid
# resolver conflicts with platform-managed packages and unnecessary optional extras.
# ZeroGPU startup depends on CUDA-backed torch internals during `import spaces`,
# so do not force the CPU-only PyTorch wheel index here.
# Keep Space-only startup trimmed to the default shape path; optional texture and
# heavy mesh-cleanup tooling is loaded lazily or skipped in fallback mode.

accelerate>=1.1.1
diffusers>=0.30.0
einops>=0.8.0
huggingface-hub>=0.30.2
imageio>=2.36.0
numpy>=1.24.4
omegaconf>=2.3.0
opencv-python-headless>=4.11.0.86
packaging>=24,<26
pillow>=10,<13
pyyaml>=6.0.2
safetensors>=0.4.4
scikit-image>=0.24.0
scipy>=1.14.1
timm>=1.0,<2
tqdm>=4.66.5
transformers>=4.46.0
trimesh>=4.4.7

torch>=2.5.1
torchvision>=0.20.1
