# Hugging Face installs from the root requirements.txt before the repo contents
# are copied into the build image, so this manifest must stay standalone.
# Keep it aligned with requirements/space.txt.

accelerate>=1.1.1
diffusers>=0.30.0
einops>=0.8.0
huggingface-hub>=0.30.2
imageio>=2.36.0
numpy>=1.24.4
omegaconf>=2.3.0
opencv-python-headless>=4.13.0.92
packaging>=24,<26
pillow>=10,<12
pyyaml>=6.0.2
safetensors>=0.4.4
scikit-image>=0.24.0
scipy>=1.14.1
timm>=1.0,<2
tqdm>=4.66.5
transformers>=4.46.0
trimesh>=4.4.7

torch>=2.5.1
torchvision>=0.20.1


